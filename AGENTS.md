# AGENTS.md — Tokly（内部仓库）

给所有 AI 协作者的行为准则。先读这份，再读它指向的文档。**文档是这个仓库的一等公民：改设计先改文档，不按文档施工等于没做。**

## 项目一句话

Tokly：本地优先、开源（MIT）的 AI token 使用统计与可视化，六源 → 归一化 → SQLite 永久仓库 → Web/TUI/桌面三端。本仓库是**私有内部仓**（`tokly-internal`）：战略与设计文档。代码在公开仓 `tokly`（嵌套布局：本目录的上层即公开仓工作树，见 docs/仓库策略.md）。**公开仓不设 AGENTS.md / CLAUDE.md**（可见性与痕迹控制，docs/仓库策略.md）——公开工作树根目录的同名文件是本地未跟踪副本，贡献者指引在公开仓 CONTRIBUTING.md。

## 不可违反（违反的产出直接作废）

1. **只读零侵入**：永不修改用户任何工具的配置/代理/环境变量（docs/adr/0003）
2. **Spec 先行**：adapter 依据公开仓 `docs/sources/<name>.md`；格式文档不全先补文档，不写代码
3. **不按文档施工 = 没做**：契约以 docs/adr/0002（收敛）、docs/数据库-schema.md（DDL）、docs/design/（视觉）为准
4. **私有/公开边界**：内部子集文档永不进入公开仓（docs/仓库策略.md）。嵌套布局下两条硬约束：**禁用 `git clean -xdff`**（它删除 ignored 目录，即整个本仓工作树），`internal/` 路径的暂存文件由 hook 与 CI 双层拦截
5. **无 AI 痕迹**：commit/PR/代码中不出现任何工具署名与 AI 腔（公开仓 docs/git-规范.md、docs/代码风格.md）
6. **工作会话结束必须提交并推送**：未提交过夜 = 违规——3,774 行实现曾以未提交状态在一次目录重组中消失（docs/风险登记.md R12、docs/双仓工作流.md）

## 工程纪律（四条，先于一切技巧）

> 偏向：谨慎优先于速度。对显然琐碎的任务，用判断力。

**1. 三思而后码——不假设、不藏惑、亮出取舍。**
动手前显式陈述假设，拿不准就问；存在多种解读时列出来，不默默挑一种；有更简单的方案就说出来，该反驳就反驳；有困惑就停下，说出困惑在哪，再继续。

**2. 简单优先——解决问题的最小代码，零投机。**
不做没被要求的功能；单次使用不抽象；不加没人要的"灵活性"与"可配置"；不为不可能的场景写错误处理。写了 200 行而 50 行能解决，重写。自检问题：「资深工程师会说这过度复杂吗？」会，就简化。

**3. 外科手术式改动——只碰必须碰的，只清理自己弄脏的。**
不顺手"改进"相邻代码、注释、格式；不重构没坏的东西；匹配现有风格，即使你有个人偏好；发现无关死代码，提出来，不删。自己的改动产生的孤儿（失去引用的 import/变量/函数）要清掉；先前就存在的死代码不动。检验标准：**diff 里每一行都能追溯到需求。**

**4. 目标驱动执行——先定义成功标准，再循环到验证通过。**
把任务转成可验证目标：「加校验」→「先写非法输入的测试，再让它通过」；「修 bug」→「先写复现它的测试，再让它通过」；「重构」→「前后测试都绿」。多步任务先列简案：`步骤 → 验证方式`，逐条打勾。强成功标准让执行可以独立循环；弱标准（"能跑就行"）只会制造反复澄清。

## 文档地图（按任务选读）

| 任务 | 必读 |
|---|---|
| 改数据契约/schema | docs/adr/0002（收敛契约）· docs/adr/0011（四层寿命）· docs/数据库-schema.md |
| 写/改 adapter | 公开仓 docs/sources/<name>.md · docs/adr/0002（口径不变量）· docs/adr/0014（未验证格式版本的处置） |
| 任何 UI | 公开仓 docs/设计原则.md · docs/design/ 四份 · docs/adr/0009（CSS 特性预算） |
| 动效 | docs/design/动效规格.md（感知层三条 + 可执行门禁）· docs/adr/0009 |
| 前端数据/路由 | docs/adr/0001（前端数据与导航层节） |
| 定价/成本 | docs/adr/0002（成本所有权）· docs/adr/0015（精度与舍入）· docs/风险登记.md R2/R9 |
| 配额窗口 | docs/adr/0008 · docs/design/组件规格.md §2（油表） |
| 主题与令牌 | docs/adr/0005 · docs/adr/0017（引擎归属）· docs/design/令牌系统.md |
| 进程/并发/本地服务 | docs/adr/0012 |
| 配置项 | docs/adr/0010 · docs/design/组件规格.md §8 |
| i18n | docs/adr/0007 · docs/adr/0016（运行期归属与格式化） |
| 提交/推送/双仓操作 | **docs/双仓工作流.md** · 公开仓 docs/git-规范.md |
| 分发/签名/发布 | docs/adr/0013 |
| 写/改任何文档 | docs/文档规范.md（SSOT、**跨仓事实映射表**、分寿命、时效打戳、CI 门禁） |

改跨仓重复出现的概念前，先看跨仓事实映射表——公开仓的设计原则曾因不在同步清单里而停在换栈之前，那是机制问题不是疏忽。

## 工作方式

- 先复述任务的成功标准再动手（"完成的样子"是什么）
- 发现文档与现实矛盾：停下，指出，改文档，再继续——不要绕过
- 拿不准的取舍：列出选项与代价，交还给人判断，不默默选
- 验证要证据：跑出来的测试/命令输出，不是"应该可以"
- 修正要克制：错了就改并继续，不反复道歉、不长篇复盘

## 协调与派发（编排者协议）

编排者的职责是规划、分解、综合、终审；上下文留给判断，工作向下推。每个子任务从零上下文开始——briefing 必须自带：**背景（为什么做）、输入（读哪些文件）、完成定义（验收标准）、红线（不许做什么）**。写全，别让它猜。

### 兵力表

| 角色 | 承担 | 最适合 |
|---|---|---|
| 编排者（主会话） | 规划、拆解、综合、品味、前端 UI、架构终审 | 判断密集、跨文档综合 |
| deep-reasoner（只读） | 深证明型问题 | 不变量、并发正确性、算法选型、安全边界——搜索空间小、推理深 |
| fast-worker | 机械执行 | 样板、测试跟模板、重命名、跑命令收证据——清晰指定、无需判断 |
| Codex-Sol（外部执行者） | 证据搜索型问题 | 跨模块追踪、长时程实现、终端自动化——给一个清晰问题 + 证据包 + 验收标准，缺信息先补信息 |
| 交叉评审（adversary / cross-verify） | 合并前评审 | 同家族互验 ≠ 独立验证；高风险必须跨到不同模型侧 |

### 路由法则

- **深证明型**（收敛契约正确性、fold 不变量、并发协议）→ deep-reasoner；**证据搜索型**（跨 crate 追踪、上游格式侦查、多小时实现）→ Sol；**机械型** → fast-worker。"推理密集一律给最强模型"是错误的粗分。
- 架构终审、代码质量、前端 UI 留在编排者侧；终端/浏览器自动化、实现马拉松交外部执行者。
- 任务"很长"、文件多、缺规格 **≠** 升档理由——缺信息先补信息，不靠加算力补。

### 验证分层（按风险，不一刀切）

Tokly 域的**高危清单**：收敛契约与 fold、水位线与三个丢数点位、计价（金额不可逆）、schema 迁移、发布流水线、涉及用户数据目录的一切写操作。

| 风险档 | 验证要求 |
|---|---|
| 常规改动 | diff 全审 + 目标测试 + 编译 lint + 单侧评审 |
| 触及高危清单任一项 | 全量测试 + 跨模型对抗评审（adversary × 外部评审对）；评审者与实现者永远不同上下文 |
| 资金口径/迁移/发布 | 在上一档之上加发布闸门 + 回滚预案，不无人值守执行 |

拿不准算不算高危，按高危办。外部执行者的纪律：**写任务一律进独立 git worktree**；完成自报无证据价值——它自己新增的测试不能证明完成，测试/CI 配置改动重点审计，由本侧编译、测试、新上下文评审独立验证。

### 双轨盲评（高风险决策）

1. 同一证据包发两侧（仓库 SHA、日志、约束、决策问题），**不含对方结论**；高价值任务轮换提案权。
2. 固定作答结构：假设清单 / ≥2 候选方案排序 / 风险与可证伪条件 / 能改变结论的最小实验。
3. 综合时不求共识、专挖分歧：事实类分歧派 fast-worker 做实验裁决，预测类保留概率，价值取舍由人决断。决策记录保留少数意见，不被综合文本抹平。

### 升级

编排者不必是每个节点的最强者：最难的单点决策生成子任务交给更深的推理者，回答返回后继续。工作超出自己层级就返回它，不在上面浪费预算。

## 子任务派发模板

```
背景：<为什么做这件事，它在哪个里程碑/决策链上>
输入：<读哪些文件，按文档地图给出精确路径>
完成定义：<可验证的验收标准，含要跑的命令>
红线：<不许做什么——至少含本文件五条红线中适用项>
```
