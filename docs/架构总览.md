# Tokly 架构总览

一页看懂整个系统。细节决策见 [adr/](adr/)，数据源细节见公开仓 `docs/sources/`，物理 schema 见 [数据库-schema](数据库-schema.md)。（2026-08-02 三轮终审：栈定稿 **Rust 内核 + TypeScript 界面**，见 [ADR-0001](adr/0001-技术栈与运行时.md)；本页形态图与包职责已按终审更新。）

## 系统形态

```
 ┌─ 数据源（只读，零侵入）──────────────────────────────┐
 │ Claude Code   Codex      OpenCode    Gemini    …    │
 │ JSONL 文件    JSONL 文件  SQLite 库   chats 文件     │
 └──────┬──────────┬───────────┬───────────┬──────────┘
        │          │           │           │
   ┌────▼───┐ ┌────▼────┐ ┌────▼─────┐ ┌───▼─────┐
   │adapter │ │adapter  │ │adapter   │ │adapter  │   crates/adapter-*
   │-claude │ │-codex   │ │-opencode │ │-gemini  │   （一源一 crate：serde 流式 → UsageEvent）
   └────┬───┘ └────┬────┘ └────┬─────┘ └───┬─────┘
        └──────────┴─────┬─────┴───────────┘
                         ▼
              ┌─────────────────────┐
              │  tokly-core（Rust）  │  归一化事件模型 · 水位线增量摄取
              │  IngestEngine        │  收敛契约 upsert（身份+可比 revision 域+显式冲突）
              └──────────┬──────────┘
                         ▼
              ┌─────────────────────┐
              │  SQLite（rusqlite）   │  usage_events(projection) / sessions(派生)
              │  唯一事实源           │  conflict_observations / pending_messages
              │  ~/.local/share/…    │  tool_calls / event_charges / ingest_errors
              │  + tokly-pricing     │  watermarks / quota_snapshots（ADR-0008）
              │                      │  成本按事件时点生效价重算（pricing_history）
              └──────────┬──────────┘
                         ▼
              ┌─────────────────────┐
              │ application service  │  查询/聚合 · 写锁单 owner
              │ （Rust）             │  DTO 经 ts-rs 生成 TS 类型
              └──────────┬──────────┘
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
  ┌───────────┐   ┌───────────┐   ┌───────────────┐
  │ Axum      │   │ Ratatui   │   │ Tauri 2       │
  │ HTTP+SSE  │   │ TUI       │   │ 直嵌 core      │
  │→浏览器SPA │   │(终端监控) │   │IPC/events→窗口│
  └───────────┘   └───────────┘   └───────────────┘
        └────────────┬─────────────┘
                     ▼
        design-tokens（DTCG 源，构建期三产物：CSS 变量 / Rust 常量 / TUI 主题）
```

## 关键数据流

1. **摄取**：`tokly scan` → adapter 发现数据文件 → 水位线跳过已读 → serde 流式解析为 UsageEvent → 批量事务入库（收敛契约：事件身份 + 可比 revision 域 + 显式冲突，事件/子表/session dirty/水位线同事务）→ 受影响 sessionKey 两侧局部重算 sessions
2. **重算**：`tokly reprice` → 按事件时刻匹配 `pricing_history` 生效区间定价 × 事件归一化 token 列 → 更新 costComputed 列（pricing worker 独占，adapter 重扫不覆盖）→ sessions 局部重算
3. **展示**：三端查询同一 application service——浏览器经 Axum HTTP+SSE，桌面窗口经 Tauri IPC/events（进程内），TUI 同进程直调（自身为 daemon 时）或连接既有 daemon、降级只读直连 SQLite；查询一律走聚合与索引

## 包职责

| crate / 包 | 职责 | 边界 |
|---|---|---|
| `tokly-core` | UsageEvent 类型、SQLite（rusqlite + 迁移）、IngestEngine | 不识任何源格式 |
| `tokly-pricing` | 定价表加载链（快照<缓存<覆盖）、成本计算、版本标识 | 纯函数，无 IO 依赖注入 |
| `tokly-adapter-*` | 一源一 crate：discover + parse（serde 流式）+ 能力声明 | 只读源数据，不写 |
| `tokly-service` | application service：查询/聚合、写锁单 owner、i18n（fluent-bundle）；DTO 经 ts-rs/typeshare 出 TS 类型 | transport 无关 |
| `tokly-server` | Axum + SSE transport（仅 127.0.0.1 + 随机 token） | 薄适配，无业务逻辑 |
| `tokly-cli` | `tokly scan / report / reprice / serve` | 只做编排，无业务逻辑 |
| `tokly-tui` | 终端监控（Ratatui + Crossterm，同进程调 service） | 同上 |
| `apps/web` | Dashboard SPA（Vite + React 19 + React Aria + ECharts 6 懒加载） | 经 HTTP/SSE 取数；DTO 用生成类型，禁止手抄领域模型 |
| `apps/desktop` | Tauri 壳（托盘/Widget/窗口，直嵌 service） | 经 IPC/events；复用 web 构建产物 |
| `packages/design-tokens`（构建期） | DTCG 令牌源、OKLCH 派生（culori）、构建期生成 CSS/Rust/TUI 三产物、主题校验 | 无运行期 UI 框架依赖 |

数据面补充（ADR-0002）：`usage_events` 是事件主表，也是**观测/投影两层模型**的投影侧——观测（observation）永久保留，**缺席 ≠ 删除**，只有显式删除证据才把投影标 `tombstoned`；同坐标或跨流同键不同 payload 一律进 `conflict_observations` 显式处理，**禁止静默 last-write-wins**（二轮审查 §1）。`tool_calls`（工具调用，含 status/时长）与 `event_charges`（非 token 计费项，如 premium-request 数）是以事件身份关联的子表，替代"零 token 独立事件"；`pending_messages` 承载流式消息的定稿状态机；`ingest_errors` 让解析坏行/跳过行可见（只存错误码/位置/hash/长度/计数，不存内容）；`quota_snapshots` 承载订阅配额窗口（[ADR-0008](adr/0008-配额窗口.md)）。

## 进程模型（2026-08-02 审查 §三 P1-11 / §四 盲区 8；三轮终审栈切换后保留——单 writer owner 自然落在 Rust application service）

**写入单 owner：单 daemon 所有权。**

- 第一个启动的进程（`tokly serve` / TUI / 桌面端内嵌 service，任一）成为 **daemon**：独占摄取与写库，托管本地 HTTP 服务（仅 127.0.0.1）。service 以库形式内嵌于各进程，无 sidecar（ADR-0004）。
- **端口文件发现**：daemon 把 `{port, pid, token, schema_version}` 写到用户数据目录的 `daemon.json`；后启动的端发现已有 daemon 即连接之，不各自起摄取。
- **TUI 降级**：daemon 不可达或版本协商失败时，TUI 降级为**只读直连** SQLite（`mode=ro`）——能看历史，不做摄取，并在界面标注降级态。
- **摄取节奏**：文件源走 watcher（fsnotify，写爆发期 debounce ~2s）；不支持 watch 的走轮询兜底（Cursor `state.vscdb` 轮询 mtime；API 源按周期幂等拉取，详见各源 spec）。
- **schema_version 协商**：连接握手时比对库版本，版本漂移**拒开**并提示迁移/升级——防新旧版本混写同一库。
- **本地服务鉴权**：127.0.0.1 上任意进程都能读端口，无鉴权等于把全部用量与项目路径暴露给本机其他进程（审查 §四 盲区 8）。首次启动生成**随机 token** 写入 `daemon.json`（文件权限仅用户可读），本地 API 一律要求携带。

## 不可违反的架构约束

1. **只读零侵入**（[ADR-0003](adr/0003-只读采集原则.md)）：对源工具零写入零配置变更
2. **只存指标不存内容**（[ADR-0002](adr/0002-数据架构.md)）：永不存储对话内容
3. **摄取收敛性幂等**：同一固定 observation 集合，交换/乱序/重放/重建四场景终态逐字节一致（ADR-0002 收敛契约 + proptest）
4. **core 不识源格式，adapter 不写库**：两者经 UsageEvent 单向解耦
5. **三端不各自实现展示逻辑**：共享 design-tokens 与 application service 查询层
6. **写入单 owner**：一切摄取经 daemon，其余端连接或只读降级（见「进程模型」）

## 性能预算（实测依据见 ADR-0001 spike 注记）

- 增量扫描：< 1s（水位线命中）；首次全量导入：万级文件 < 30s
- Dashboard 任意聚合查询：< 100ms（`PRAGMA cache_size=-16000` 已固化）
- 数据库体积：**三年重度使用 GB 级**（2026-08-02 审查 §四 7 修正，原「每年 < 100MB」口径不含索引与页开销，乐观 5–10 倍；缓解措施见 ADR-0002 资源纪律）；内存：解析期恒定（流式）
