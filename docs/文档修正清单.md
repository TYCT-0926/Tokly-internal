# 文档修正清单（灾后重建 2026-08-04）

审查-2026-08-03 产出的文档修正项。原"应用到公开仓"的定位随单仓改版作废——文档已全部统一在 `internal/docs/`，条目改为**内部文档修正**，由[执行计划](执行计划.md)对应阶段消化。公开仓根文件（README / SECURITY / CONTRIBUTING / 模板）已由维护者重写完毕，相关条目关闭。

## 已完成

- 防线与痕迹：单仓设置、干净单提交历史（`7b1dbcc`，noreply 作者，只含项目文件）、AGENTS/CLAUDE/docs 三重守卫（ignore + exclude + hook）
- 公开根文件：README（保留边界措辞按 ADR-0011 收窄、去文档链接）、CONTRIBUTING（自包含、无 AI 字样）、PR 模板、issue 模板
- 阶段一（工程地基）：开发流程 CI 节补 guard / cargo-deny；代码风格增 unsafe 与 MSRV 政策、三个审计命令；git-规范增署名机器拦截、私有路径、禁 `clean -xdff`

## → 阶段二 / 三B（已消化）

六源 spec 逐份对齐规范 DDL 的六条核对项（源标识格式约束、projectKey/Label、cacheWrite 双档、双成本列 + 四口径列、子表模型、已验证格式版本集合节）——阶段二任务 A 完成，三B 十一项收敛与 cursor eventKey 二轮裁决已叠加。

## → 阶段五（已执行 2026-08-03）

设计原则.md 七处修正全部完成：[x] uPlot → ECharts 6 · [x] 补 sakura（五套）· [x] 删孤值 `#0E0C0A` · [x] 卡片圆角 10px · [x] stagger 参数改热力图每列 8ms · [x] 签名时刻唯一化 · [x] 信息架构第一屏能力驱动；另 [x] 溯源标注五档、[x] 保留边界诚实呈现两段补入。

> 2026-08-04 补充：表现力预算由「一个时刻」扩为「**一线一点**」（一条首载编排 + 热力图签名对象），细则见 [design/编排规格.md](design/编排规格.md)；设计原则个性预算段与动效规格已同步。

## → 阶段七B（落地时验证）

- SECURITY.md 深化：本地服务威胁模型（token 权限 / Origin 校验含缺失语义 / fragment 传递）——按 ADR-0012 定案形态写，待 server 面向用户暴露时同批。
