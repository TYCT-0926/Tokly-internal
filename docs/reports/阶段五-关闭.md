# 阶段五 · 关闭记录

- 日期：2026-08-04
- 结论：**关闭**。squash 合入 main（`b660db6`），分支与 worktree 已清。合并前维护者亲自执行：`cargo xtask tokens` 十变体全过门零救济 → specimen 页产出核验 → rebase 到含阶段六的 main（Cargo.toml/lock 机械冲突，取并集后重建 lockfile）→ 全链 **150 测试全过、deny 四项 ok** → PR #3 CI 三平台 + desktop + guard + deny 全 SUCCESS。

## 交付

`crates/tokly-tokens`：DTCG 解析、OKLCH 派生、gamut 映射、对比度救济、质量门、ANSI 降级链、`linear()` 弹簧采样——**唯一实现在 Rust**，同一份代码服务构建期三产物与 `tokly theme install` 的用户主题派生（ADR-0017）。`packages/design-tokens`：DTCG 令牌源 + 真实字体（Geist Sans v1.7.2 / Maple Mono v7.9，OFL 随库）+ 七节 specimen 页。10 个官方变体的 golden JSON/CSS 入库为唯一验收面。

## 验证结构（本项目迄今最强，记为算法类阶段的参照标准）

三层独立验证：① 另一上下文从规格出发的 Python 参照实现，与 Rust 引擎 **1,188 项数值逐项一致**（全部 hex、对比度、heat、ANSI256/16、弹簧 57 停靠点）；② 对抗式规格审查逐行对照公式，零偏离，但攻破两处工程面（主题 id 不查重、gamut 门失败不点名）；③ 全新上下文复跑完成定义九项全过，生成的 `tokens.rs` 另经 rustc 独立编译验证。

## 裁决与人工验收

四项设计裁决与二轮规格修复见[阶段五-裁决](阶段五-裁决.md)。**维护者 specimen 人工验收：通过**（"大体上没什么太大的问题"）——四定点未提出异议，`series[3]` 与 `fg.primary` 在 mono-dark 下的相邻度（OKLab ≈0.012）按现值保留。

## 常设资产

**P-tokens 全称性质**：任意通过 Tier 1 校验的种子 → 每个产出令牌量化前通道合法，或该种子被显式拒绝；不存在"过门但被静默钳制"的第三态。突变自证抓出**底端**负 L 逃逸（`oklch(−0.105724 0 0)`，与复审的顶端反例不同门——真防线的直接证据），收缩反例入 proptest-regressions 常驻重放。

## 界面门状态

spike ② 的令牌基座部分完成。界面门余 **七A（UX 竖切）**——与 spike ③（阶段六，已完成）齐备后一并评审，卡 M2 Dashboard 施工。
