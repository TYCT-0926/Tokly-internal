# 阶段六 · 关闭记录

- 日期：2026-08-04
- 结论：**关闭**。PR #2（20 提交）squash 合入 main（`1396bea`），远端与本地分支已删、worktree 已清（同批顺手清理阶段二遗留的 `Tokly-final-mutation-smoke` 游离 worktree）。合并前终检：MERGEABLE·CLEAN，rust 三平台 / guard / cargo-deny / desktop 全 SUCCESS，web 按预期 skipped。

## 交付

同一 application service 同时接 **Axum HTTP+SSE / Ratatui 同进程 / Tauri IPC** 三 transport，三端对同一查询输出逐字段相等（HTTP 侧跨进程与进程内双向闭环，Tauri 侧另断言 JSON 字节级一致）。写入单 owner 由 OS 咨询文件锁在三个真实子进程场景证成（抢锁失败转连接、kill 后自动释放、并发双启动恰一胜）。本机实测：NSIS 安装包 **2.50 MB**（ADR-0004 目标 ~10MB 级，低一个档位）、冷启动至首窗 306ms。

## 三轮裁决与本阶段的通则产出

| 轮 | 裁决 |
|---|---|
| 一轮（8 项） | `Origin` 缺失放行至 token 层（**转述失真归维护侧**，ADR 原文只写强制校验）；锁保证边界接受同用户破坏 + 周期核验兜底，不实施 share-mode；DACL 硬化必须 reset + 读回 + fail-closed 且有能变红的权限测试 |
| 二轮（3 项） | lock-integrity **不持久化**（我的 `ingest_errors` 条款撤回——该表身份模型全是源作用域，daemon 级事件无一维可填）；桌面树补独立 CI job；POSIX ACL 与 Windows 对等，验收面在 Linux runner |
| 三轮（2 项） | advisories 取中间形态（vulnerability/yanked 拒、unmaintained 警告**逐条打印**，不写 ignore——警报器不拆）；Linux 突变自证接受证据链，不为边际确定性污染 PR 历史，挂 M5 多平台 runner |

**跨模型评审的价值实证**：`icacls /grant:r` 只替换目标 SID 自身 grant、其他主体显式 ACE 存活（硬化"报告成功但目录仍公开"）——Claude 侧 20 agent 四镜头未命中，Codex-Sol 与新上下文 Opus 互不可见双双独立命中并指向同一更优方案，执行者实测复现。这是「同家族互验 ≠ 独立验证」的直接证据。

**通则入约**（ADR-0012 + 开发流程）：权限/环境类代码的正确性依赖宿主目录形态，单机验证覆盖面天然不足 → 必须跨形态 CI 验证；此类代码必须有"删掉硬化调用即变红"的测试。由 CI 实际抓到的缺陷换来（同一份代码在有可继承 ACE 的 profile 目录下永远正确、在无继承目录下走令牌默认 DACL 即漏）。

## 界面门状态

spike ③ 完成。界面门余 **spike ②（七A UX 竖切）**，两者齐备后一并评审 → 卡 M2 Dashboard 施工。

## 已知边界（挂账）

- Linux 侧"删掉硬化调用即变红"的突变自证 → M5 多平台 runner；
- 桌面树 16 条 unmaintained（零漏洞、无升级路径）→ M5 复核数量与构成，显著上升即 Tauri 生态退化信号；
- Codex 本机沙箱损坏（`orchestrator_helper_incomplete`）→ 派复审前需修；本阶段评审经内联代码绕行完成。
