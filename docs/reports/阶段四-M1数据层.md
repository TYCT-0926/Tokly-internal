# 阶段四 M1 全量数据层 —— 执行报告

> **本文件状态提示（请维护者优先处理）**：本轮开始时本目录存在 `阶段四-M1数据层.md`（20,935 字节，08-05 00:40），记录批 1/2/2.5/3/点5 的执行过程。本轮写作时该文件已不在磁盘上。排查结论见 §七「事故与卫生」——**它从未被 internal 仓跟踪**，而 internal 仓在本轮期间被重新 clone（`git reflog` 显示 `HEAD@{1}: clone`），未跟踪文件因此随之消失；同目录的 `阶段四-复审.md` 因已跟踪而完好。本文件为**本轮（对抗复审修复轮）的新报告**，不含前几轮的过程记录。**未跟踪 = 不存在，请尽快提交。**

- 日期：2026-08-05
- 分支：`feat/m1-data`（PR #7，未 self-merge）
- 起点：`86171b8`
- 本轮提交：`457966d`、`ccc9c05`、`d4dc49f`、`730d683`、`b18d521`
- 依据：[阶段四-复审](阶段四-复审.md) 16 项发现 A–H；[ADR-0002](../adr/0002-数据架构.md) 定案 1–6 及两节补文；[ADR-0015](../adr/0015-计价精度.md)、[ADR-0019](../adr/0019-数据新鲜度与变更传播.md)、[ADR-0011](../adr/0011-存储生命周期.md)

---

## 一、先说三条必须裁决的事

这三条是本轮**发现的契约缺口 / 规格与实现不可两全处**。按纪律没有静默取舍，全部在此上报，请维护者裁决。

### 缺口 1：定案 6 补文第 3 条的 `skipped` 第三因，其「充要」刻画并不互斥（已按窄化实现，需追认）

补文原文把第三因的谓词写成双条件：

> 「该事件在冻结事务中成为基线成员且当时 `computed_cost_status='pending'`」⟺ 「终态 `skipped` 且金额与 `pricing_version` 均 NULL 且所属会话 `has_partial_cost=1`」

右侧**不是**第三因的专有特征。一个第一因事件（`pricing_eligibility='disabled'`，由 worker 落章 `skipped`、金额与版本均 NULL）只要恰好落在一个因别的原因被标 `has_partial_cost=1` 的会话里，就**逐字满足右侧而不满足左侧**——而这种会话在实测中很常见（`rebuild_session` 只要该会话计价覆盖不齐就置 1）。所以该双条件按字面为**假**。

实现取舍（已落地，明确上报而非静默）：判定加入 `frozen_baseline_at IS NOT NULL`，即以「冻结事务写下的成员标记」作为第三因的判据。这是**窄化**，理由是左侧本身无歧义（基线成员 + 当时 pending），右侧只是它的一个不充分刻画。测试文件里已写明这是有意窄化。

请裁决：把补文右侧改为含 `frozen_baseline_at` 的刻画，还是另立区分方式。位置：`crates/tokly-pricing/tests/skipped_causes.rs`。

### 缺口 2：`skipped` 三因之二「配置关闭计价」在 M1 无任何写入者

ADR-0015 列 `skipped` 三因：`pricing_eligibility` 判定 / **配置关闭** / 生命周期封存。全仓审计结果：`tokly-config` 只有 `pricing.remote_refresh` 与 `pricing.override_path` 两个键，**没有任何「关闭计价」的配置面**，因此第二因没有产生者，其谓词恒假。

这不构成契约违反（空析取项不会让枚举出错），但使「三因枚举」实际只有两因。P6 的 `skipped` 枚举里已把第二因显式写成空集并注明。请裁决：补配置键，还是把 ADR-0015 的三因改为两因。

### 缺口 3：ADR-0019 的 `scope = health` 标注没有产生者（DTO 有，发布者无）

定案 3 要求诊断可见写「事件 scope 标 `health`」。实测：`tokly-service` 的 `ChangeScope`（含 `Health`）与 `ChangeEvent` 两个 DTO **已定义**，但全仓**没有任何代码构造 ChangeEvent**——其文档注释自己写着「Publishers arrive with the ingest path and the SSE upgrade」，即发布者属后续阶段。

本轮落地的是**数据层部分**（谓词域扩为 fold ∪ health，单一计数器递增），未新增 scope 标注机制（无产生者可标，且新增广播层不属 M1 数据层范围，必要性检验不过）。请确认这一分界，或指定由哪个阶段补发布者。

---

## 二、批 A —— 点 6 模型手术与 G2 全证据码（`457966d`）

### 做了什么

`crates/tokly-core/tests/common/model.rs` 重写。契约模型现在按定案 6 补文直译出**四类结局**，判定序固定为 pruned > 基线 > 定案 5 三分：

- `PrunedSkip`：投影不可变，留 `pruned-key-reobserved`
- `BaselineSkip`：**新增第四类**，全列零触碰（含 computed 三列），**零证据**
- `ZeroTouchSkip` / `CanonicalizingWrite` / `ContentWrite`：定案 5 的 (a)(b)(c)

为区分 (b) 与 (a)，模型新增胜者**坐标**跟踪（此前只跟踪胜者派生内容，(b) 类只能靠时间列间接落到「非跳过」，属侥幸正确）。

P6 侧：`expected_ingest_errors()` 统一从一张 `errors` 表派生，两种计数语义分开——按到达计数（schema 注明「重扫同位置同错误计数 +1」）与按合成 observation 集合基数计数（证据字段补文对 `api-reconcile-absent`/`pending-vanished` 的规定）。

### 恢复为永久回归的项

| 测试 | 位置 | 覆盖 |
|---|---|---|
| `p5_skip_outcomes_have_zero_side_effects` | convergence.rs | P5 四类结局；freeze 进入生成历史 |
| `p5_generator_reaches_every_outcome_class` | convergence.rs | 生成器必须触达每一类，否则红 |
| `p6_every_evidence_row_is_justified_and_every_predicate_has_its_row` | evidence.rs | 两表全表双向比较 |
| `p6_generator_reaches_every_corner` | evidence.rs | **改用真模型**驱动，删掉原「简化计数器」副本 |
| `the_prefix_puts_the_reconcile_and_vanish_codes_on_record` | evidence.rs | 十码共存于同一终态 |
| `a_sealed_baseline_absorbs_neither_a_replay_nor_a_rewrite` | fold_rules.rs | 语料 (a)(b)：**同键先证 (c) 覆盖、封存后同形到达零触碰** |
| `a_frozen_session_takes_a_new_key_on_its_tail_and_leaves_its_baseline_closed` | fold_rules.rs | 语料 (c) 尾侧 + 基线零证据 |
| `a_full_rebuild_leaves_a_sealed_baseline_alone` | fold_rules.rs | **新发现缺陷**（下） |
| `a_freeze_leaves_nothing_queued_behind_it_whatever_its_eligibility` | compact_lifecycle.rs | **新发现缺陷**（下） |
| `skipped_is_exactly_the_causes_the_contract_names` | tokly-pricing/tests/skipped_causes.rs | 语料 (d) + 三因双向枚举 |

G2 要求的十个证据码全部由生成器可达并双向比较：`json-parse`、`unverified-schema`、`unresolved-mapping`（新增 `AdapterError` 算子）、`unresolved-semantics`（新增外部冲突算子，fold 既不采纳也不删除）、两类 fold 冲突、`normalizer-divergence`、`pruned-key-reobserved`、`frozen-session-reactivated`、`api-reconcile-absent` 与 `pending-vanished`（固定前缀驱动，使全表比较对十码都成立而非只对 fold 写的八码成立）。

### 加宽的 oracle 抓到的两个真实缺陷

**A-1 全量重建抹掉基线封存（严重）。** `rebuild_projection_from_observations` 以 `observations_pruned_at IS NULL` 为界删除并重折叠投影，基线事件正在此列内。后果两层：一是**重写了已入永久汇总的内容**（违反「此后零触碰绝对」）；二是**丢掉 `frozen_baseline_at` 标记本身**——而尾账正是靠该标记为空来识别尾侧事件，所以重建后的基线事件会被下一次 compact **吸收进尾账，与已包含它的汇总重复计数**。修法：重建的四条语句统一以 `COALESCE(observations_pruned_at, frozen_baseline_at) IS NOT NULL` 为不可变界，与 `fold_key` 既有判据一致。

复现（修前红）：
```
cargo test -p tokly-core --test fold_rules a_full_rebuild_leaves_a_sealed_baseline_alone
```
修前实际：`("...0020","h03",None,103)`，期望 `("...0010","h00",Some(1722000000001),100)`。

**A-2 冻结把不可计价事件永久留在 `pending`（违反 ADR-0015 红线）。** 冻结落章语句带 `AND pricing_eligibility <> 'disabled'`，而 reprice 的分页扫描又排除基线成员——两者相加，一个 `disabled` 事件若在 worker 首扫前随会话冻结，就**永远停在 `pending`**，正是 ADR-0015「不参与计价的事件永停 pending 即谎报队列，禁止」所禁。补文第 3 条左侧只说「基线成员且当时 pending」，**未按资格收窄**，故实现的 `<> 'disabled'` 是无授权的窄化。修法：去掉该条件，终态同为 `skipped` + 金额/版本 NULL。

### 门禁
`cargo run -p xtask -- ci`：fmt / clippy -D warnings / **462 通过 1 skipped** / build 全绿；`cargo deny check` 四项 ok；桌面树 `apps/desktop/src-tauri` 单独 `cargo test --locked` 1 通过。

---

## 三、批 B —— F1 第四腿与 F2 部分成本（`ccc9c05`）

### F1 第四腿

`store_totals` 由三腿改为**五段四腿**：净汇总 + 脏会话事件 + 无会话事件 + **（存活尾侧事件 + `session_tail_ledger`）**。

四腿的识别谓词：会话 `frozen_at IS NOT NULL` 且事件 `frozen_baseline_at IS NULL`。同时给脏腿补 `AND s.frozen_at IS NULL`，使四腿**可证不相交**（冻结会话由「定格汇总 + 尾账」核算，不再有与脏腿重叠的可能）。

尾账读回时用 `native_cost_events` / `computed_cost_events` 两个计数列把 `NOT NULL DEFAULT 0` 的金额还原成 `Option`——否则「无成本记录」会显示成 `$0.00`，这正是 schema 注释里那两列存在的理由。

复审给的最小反例已恢复为永久回归，且**修前实测与复审报告逐字段一致**：

```
cargo test -p tokly-cli report::tests::a_reactivated_frozen_session_does_not_hide_the_new_event
```
修前：`events 4 / input 737 / computed 1,000,000`；真值：`5 / 778 / 1,000,017`。

另加 `an_absorbed_tail_event_survives_in_the_ledger`：先断言活尾事件确实进了总账（锚定 sealed 基线，避免两端同时丢失而假绿），再模拟 compact「同事务累加后删除」，断言总账不变。

### F2 部分成本

`Totals` 增 `partial_cost`。三种来源：汇总层用 `has_partial_cost` 与「跨会话有/无成本混合」；事件腿与尾账用覆盖计数比较。`merge` 另行检测**跨腿缺口**（一腿全有价、另一腿全无价，各自内部都不混合，合起来仍是部分覆盖）。

展示：部分金额单元格带 `*` 标记（直接贴在数字上，二者不可分读），表下给出解释行。既有测试 `the_table_separates_the_two_costs_and_marks_the_missing_one` 的期望随之更新为 `["-", "$1.25*", "$6.25*"]` / `["$0.60", "$1.00*", "$1.00*"]`——今日列单事件、两列各自覆盖完整，故不带标记，这一点也一并断言。

### 门禁
`xtask ci` **465 通过 1 skipped** 全绿；`cargo deny check` ok。

---

## 四、批 C —— E1 幽灵聚合与 D1 健康版本（`d4dc49f`）

### E1

`write_usage_rollups` / `write_tool_rollups` 改为「先把聚合集合物化进临时表，再 upsert，再删除当前集合中缺席的非 frozen 键」。frozen 键继续跳过。`CompactReport` 增 `stale_rollup_keys_removed` 并计入 `changed()`（否则会出现「删了行却不推进 `data_version`」，正是 D1 那一类缺陷）。

复现（修前红）：
```
cargo test -p tokly-core --test compact_lifecycle recompute_removes_non_frozen_rollup_keys_absent_from_the_projection
```
断言写成「聚合行数 = 投影当前产生的键数」等四条，对着投影而不是对着字面值，所以它说的是契约而不是这个 fixture 恰好长什么样。

### D1

按定案 3 与 ADR-0019 §1，递增谓词由 fold 域扩为 fold ∪ health 域，**不另立健康版本**。全仓独立 writer 审计后逐个定性：

| writer | 是否递增 | 依据 |
|---|---|---|
| `record_ingest_error` | 是（恒） | doctor 打印错误码/计数/末次时间 |
| `advance_watermark` | 仅当游标真的前进 | 单调规则拒绝的推进不写行 |
| `record_stream_stamp` | 仅当 stamp 真的变化 | 同上 |
| `ingest_batch` / `publish_api_period` / `finalize_*` | 批内共享一次递增，游标移动并入 `batch_touched` | 定案 3「批内共享批提交的一次递增」 |
| `resolve_instance` | **仅**首次出现或 `format_version` 改变 | doctor 读 source/id/data_root/format_version，**不读 `last_seen_at`**；若为存活时间戳递增，则每轮空扫都会失效全部客户端缓存 |
| `pending` 写 | 已有（`observe_pending` 条件递增） | — |

复审反例已恢复：`a_new_health_finding_advances_the_generation`（修前 count 0→1 而版本仍 0）。另新增 `a_refused_watermark_advance_does_not_advance_the_generation`、`a_stream_stamp_refresh_advances_the_generation`、`a_source_instance_advances_the_generation_only_when_it_appears_or_changes`、`a_pruned_key_reobservation_advances_the_generation`。

**已翻转的既有测试（明确上报）**：`a_watermark_only_advance_does_not_advance_the_generation` → `..._advances_the_generation`。它写的是旧的「游标不是查询结果」口径，与定案 3 直接冲突，属规格变更的必然后果，不是为了让实现过关而改测试。

连带地，P5 的版本断言改为精确形式：**跳过类到达为其消费的游标推进恰好一次，除此之外为零**（用 `watermarks` 单独快照判定游标是否真的动过）。`stale_normalizer_version_is_a_zero_change_skip` 的版本断言同理改为 `version + 1`。

### 门禁
`xtask ci` **470 通过 1 skipped**；`cargo deny check` ok；桌面树单独跑 1 通过。

---

## 五、批 D —— 可审计突变清单（`730d683`）

替代「18/18」的是一个**可复跑的子命令**：

```
cargo run -p xtask -- mutate            # 全部
cargo run -p xtask -- mutate compact    # 按 id 子串过滤
```

位置：`xtask/src/mutation.rs`。每个 mutant 记录：id、目标文件、**单一语义变化的完整补丁文本**（`from`/`to` 逐字写在清单里，不跑也能读）、一句话说明、预期 killer 测试、实际命令与退出码、判定。锚点必须在文件中恰好出现一次，否则运行直接失败（清单与源码脱节会被当场发现，本轮就发生过一次）。跑完校验每个被改文件都已复原，否则报错而不是留下改动。

**结果：25/25 击杀，3 个作为等价突变剔除，退出码 0。**

过程值得记录：**首轮 28 个 mutant 只杀死 17**。存活的 11 个里，8 个是我给的 killer 测试根本不可能抓到（例如用一个根本没有冻结会话的 fixture 去验尾账规则）。这些暴露的**真实覆盖缺口已全部补齐**，新增回归：两条保留期是独立时钟、尾账只收封存后到达且放过过期的基线、封存只发生一次不重复盖章、冻结不动已计算出的成本、excluded 事件不入基线、pruned 键重观测是可见变更、部分成本必须标在数字本身（原测试只断言页面里出现 "partial"，footer 存在即通过，抓不到单元格标记被移除）、尾账无成本记录不得显示为 0。

3 个等价突变**剔除而非计入击杀**，各自附不可能改变终态的论证：

1. `fold/baseline-key-is-folded`——`ingest_one` 的基线短路被去掉后，`fold_key` 仍用同一 `COALESCE(...)` 判据返回零变更，终态/证据/结局三者全同。真正承载该行为的是另一处守卫，由 `rebuild/baseline-is-rederived` 击杀。
2. `compact/baseline-remarked-on-every-pass` 与 3. `compact/sessions-refrozen`——两者都被 `compact_expiring_sessions` 的 `s.frozen_at IS NULL` 挡住：已冻结会话永不再进计划表，所以语句里的 `frozen_at IS NULL` / `frozen_baseline_at IS NULL` 是纵深防御而非承载条件。

第 2、3 条同时说明了一件对复审有用的事：**计划表的谓词才是冻结的承载点**，把纵深防御当作被杀死的 mutant 计分，正是突变分数虚高的典型来路。

---

## 六、批 E —— 性能机构（`b18d521`）

### 机构

criterion 0.8 入 `[workspace.dependencies]`（`default-features = false`，去掉 plotters/rayon；`cargo deny` 四项仍 ok）。新建 `crates/tokly-bench`：

- `src/dataset.rs`：**六源**确定性生成器。只有 claude-code 有生产 adapter，走真实 JSONL 文件；另五源在 **store 层以 `NewObservation` 直接注入**（`ingest_batch`）。**合成性质写进数据本身**——合成实例的 `data_root` 为 `synthetic://tokly-bench/<source>`，任何数据库导出都会带上这个名字，并有测试 `the_synthetic_sources_are_labelled_everywhere` 守住。种子哈希（SplitMix64 计数器）与确定性不变。
- `src/lib.rs`：`Fixture` + 普查/完整性断言 + 稳态体积测量。
- `benches/m1_data_layer.rs`：三项测量（冷首次导入、冷增量扫描、暖增量扫描）。
- `tests/integrity.rs`：**9 条证明「检查确实会失败」**的测试。

`xtask` 的 `bench-fixture` 子命令改为 `bench`（转调 `cargo bench -p tokly-bench`），生成器从 `xtask/src/bench_fixture.rs` **移出**——store 级注入需要 `tokly-core`，而 xtask 按其自身注释是刻意无依赖的（否则 `cargo run -p xtask -- fmt` 要先编译捆绑 SQLite）。原文件三条测试一并迁移，无任何文档/CI 引用遗留。

### 完整性断言（复审 H2 的核心要求）

每次测量前后都断言：事件数、观测数、会话数、`SUM(sessions.event_count)`、水位线数、0 ingest error、0 dirty session、0 非 counted 事件、6 个 source instance，外加一次空转增量扫描**必须让整份普查与 `data_version` 都不变**。**所有期望值由 scale 推导，绝不从被检库里读回**。任一失败即 panic → 非零退出、不打印任何数字。

`tests/integrity.rs` 逐条证明它们会响：截断导入（同时报出五项计数并中止）、源文件丢失、有 ingest error、有 dirty session、扫描居然真的摄取了。**复审那个 351,090 行截断样本的故障已被复现为其中一条。**

### 实测（Windows 11，bench profile，10 样本，95% CI）

| 测量 | 下界 | 估计 | 上界 |
|---|---|---|---|
| `m1/first-import/cold` | 31.833 s | **32.189 s** | 32.514 s |
| `m1/rescan/cold` | 13.583 ms | **14.407 ms** | 15.191 ms |
| `m1/rescan/warm` | 13.229 ms | **14.149 ms** | 15.109 ms |

导入独立三轮：**28.611 s** [26.887, 30.543]、**32.098 s** [31.795, 32.438]、**32.189 s** [31.833, 32.514]。后两轮互相吻合，第一轮是离群值且其上界仍越过 30 s。

### SLO 判定：**未达标（FAIL）**

口径为维护者确认的「十万级事件 < 30 s」。两轮一致结果的**整个置信区间都在 30 s 以上**。按纪律**未做任何调优、未从测量区间里排除任何步骤**，交实测分布由维护者裁定「改目标还是继续优化」。

两点必须一并读：

1. **这个数字偏乐观。** 被测的是 `tokly scan` 的完整用户可见操作（1,112 个 JSONL 文件里 16,670 条 claude-code 事件的 adapter 扫描 + 83,330 条合成事件注入 + 6,667 个会话的 `rebuild_dirty_sessions`），但**其中 83% 的事件因五源无 adapter 而完全跳过了 JSONL 解析与映射**。真正六 adapter 的十万级导入只会比 32 s 更慢。
2. **冷/暖增量扫描统计上不可区分**（置信区间重叠）。这不是测坏了，是真实结论：空转一轮的开销由 1,112 次 `stat` 主导，而本机能控制的那部分「冷」（新连接 ⇒ 空 prepared-statement 缓存与空 SQLite page cache）根本不在热点上。**「冷」无法包含操作系统页缓存的清除**——Windows 上没有可移植的做法（需管理员权限），此点已写在 `Fixture::open_cold` 的文档注释里，故该数字是真正冷启动的下界。

### compact 后稳态体积（90 天观测 / 180 天投影，UTC 日界）

| 阶段 | 数据库 | 日志（wal+shm） | 合计 |
|---|---|---|---|
| 导入后 | 205.5 MiB | 7.6 MiB | 213.1 MiB |
| compact 后 | 217.9 MiB | 111.4 MiB | 329.3 MiB |
| vacuum 后 | 40.6 MiB | 111.4 MiB | 152.0 MiB |
| **落定（checkpoint 后）** | **40.6 MiB** | 0.2 MiB | **40.8 MiB** |

10 万事件存活 16,092、10 万观测存活 7,887；冻结 5,594 个会话，写出 34,272 条 rollup。**体积按文件分列而不是相加**：在 compact 中途把两者加起来会让一次回收过程看起来像增长——「compact 后 329.3 MiB」单独拿出来说正是这种误读。

### 1.2M 全量未运行

按指示只跑到十万级。`TOKLY_BENCH_SCALE=full` 选择 1.2M/80k 全量；`Scale::parse` 有单元测试，下游一律只读 scale，tiny(600) 与 SLO(100k) 两档已走通同一条代码路径。

### 复现命令

```
CARGO_BUILD_JOBS=2 cargo bench --locked -p tokly-bench       # 约 7 分钟，三项测量 + 稳态体积
CARGO_BUILD_JOBS=2 cargo run -p xtask -- bench               # 同上，经 xtask
TOKLY_BENCH_SCALE=full cargo bench --locked -p tokly-bench   # 1.2M / 80k，本轮未跑
CARGO_BUILD_JOBS=2 cargo nextest run -p tokly-bench          # 完整性断言的「会失败」证明，15 条
```

### 顺带做的复审建议项

`PendingBatch` 现在**每个游标只保留最远的一次推进**。原实现只有 observation 才让批变满，所以一段纯 skip/坏行会一路累积到 EOF 一行一条游标。合并规则与水位线行落库时的单调规则一致（新代直接胜出，同代比位置，不可比则不动），另加 5 条单元测试。

### 门禁
`xtask ci`：fmt / clippy / **494 通过 1 skipped** / build 全绿；`cargo deny check` 四项 ok（criterion 未引入任何许可或公告问题）；桌面树单独 1 通过。

---

## 七、事故与卫生

### 前几轮执行报告丢失（未跟踪 + 重新 clone）

本轮开始时 `internal/docs/reports/阶段四-M1数据层.md` 存在（20,935 字节，08-05 00:40），写作本文时已不在磁盘。排查：

- 该文件**从未被 internal 仓跟踪**（`git log -- <path>` 为空）；
- internal 仓 `git reflog` 只有两条：`HEAD@{1}: clone`、`HEAD@{0}: commit 600219f`——即本仓在本轮期间**被重新 clone**；
- 全新 clone 只含已跟踪文件，未跟踪文件不会出现；同目录 `阶段四-复审.md` 因**已跟踪**而完好无损。

结论：与公开仓的施工无关（`/internal/` 在公开仓 `.gitignore` 内，本轮四次提交均未触及），是「未跟踪文件 + 重新 clone」的必然结果。这已经是 internal 第三次同类损失（前两次见 `600219f` 提交正文）。**建议**：internal 仓的报告文件写入后立即提交，不要以未跟踪状态跨越任何仓库级操作。本文另存一份于会话临时目录。

### 公开仓工作树

四次提交，均为中性工程语气、noreply 身份、无任何 AI 署名。突变审计跑完后 `git diff --stat crates/tokly-core/src/` 为空，确认实现源码未被审计残留改动。`target/` 下既有基准库（`bench.db` 等）未被删除——本轮只删过 `target/debug`（构建产物，可再生），基准库位于 `target/` 根。

### 发现一条既有的负载敏感 flaky 测试（不在本轮范围，建议维护者处理）

`tokly-service::service_queries::the_generation_and_the_numbers_come_from_one_commit` 在批 E 后的一次全量 `nextest` 里**失败**，随即单独跑与再次全量跑都通过——**是 flaky，不是坏了**。

机制已定位：读侧是一个**固定 400 次**的循环，与一个 `spawn_blocking` 写线程赛跑，两者之间没有任何汇合点；末尾断言 `seen_moving`。机器负载高时 400 次读可以在写线程被调度之前全部跑完，于是 `seen_moving` 恒假而失败。真正的不变量（版本与数字同属一次提交的「对角线」断言）是稳健的，脆的是 `seen_moving` 这一条。

**该测试属阶段七/九的 service 代码，本轮未触碰**；本轮把工作区测试数从 470 增至 494 只是提高了它暴露的概率，脆弱性是既有的。按「只改与请求相关的代码」未擅自修改，在此上报——修法方向是给读循环一个与写线程的汇合点（等写侧真的产生进展再计数），而不是把循环次数调大。

### 本机工具链不稳定（非代码问题，但会误导后续执行者）

本轮遇到三次不同的编译器/链接器崩溃，分别落在 `tokly-service`、`tokly-cli`、`tokly-core`，形态各异（rustc ICE、`link.exe` STATUS_STACK_BUFFER_OVERRUN、borrowck 阶段栈溢出），且伴随一次虚假的 `use std::fmt::Write` 未导入报错（该导入实际存在于第 12 行）。**降低并行度即消失**：全工作区在 `-j 2` 下稳定通过。后续执行者请用 `CARGO_BUILD_JOBS=2`，不要把这些崩溃当成自己的改动引起的。

---

## 八、复审 16 项对照

| 编号 | 本轮状态 |
|---|---|
| A1（判无洞） | 保持；建议项「限制纯 skip/坏行批内存」在批 E |
| B1 / B2 / B3 / B4 | 前几轮已修（本轮未改动，回归仍绿） |
| C1 / C2 | 前几轮已修 |
| D1 | 本轮批 C 修复 + 全 writer 审计 |
| E1 | 本轮批 C 修复 |
| F1 / F2 | 本轮批 B 修复 |
| G1 | 前几轮修复，本轮追认为定案 5 三分并加坐标跟踪 |
| G2 | 本轮批 A 修复（十码双向 + 生成器改用真模型） |
| G3 | 本轮批 D 交付可复跑清单 |
| H1 | 本轮批 E 交付（criterion + 六源 + 冷/暖 + 完整性 + 稳态体积） |
| H2 | 机构已建成并复现了截断故障；**SLO 实测未达标（32.189 s vs < 30 s），交裁决** |

本轮另新增两项复审未发现的缺陷（A-1 重建抹掉封存、A-2 永久 pending），均由加宽后的 oracle 抓出。
