# 项目时间线（活文档，随阶段关闭更新）

## 2026-08-02（本执行周期前）

设计冻结（架构 / ADR 0001-0008 / 设计图纸 / 规范）→ 外部深度审查 + 二轮独立复审 → 三轮终审定栈 **Rust 内核 + TS 界面**。

## 2026-08-03 · 文档与仓库基线日

| 时段 | 事件 |
|---|---|
| 审查 | 四轮全量审查：82 项发现 → 新增 ADR 0009-0017、修订 0001-0007、重写 schema/架构/路线图/风险/设计四份 |
| 事故 1 | 发现公开仓工作树连同 3,774 行未提交实现从磁盘消失（R12 起源；催生"未推送即不存在"全局规则） |
| 仓库演进 | 双远端仓 → 单仓模型：文档树统一进 `internal/`（私有永不提交）、公开仓收敛为纯项目文件、全新干净历史 `7b1dbcc`；防线三层（noreply 身份 / info-exclude / pre-commit hook） |
| 计划 | 执行计划十阶段（轨道：后端核心 → 设计基座与 Web → 数据扩展 → TUI → 桌面最后）；一次一阶段 + 并行规则 |
| 阶段〇 / 一 | 仓库基线关闭；脚手架 + CI 门禁经一轮复审修复后合入（`fbee54b` / `fbe8972`），NUL 守卫后续修为 `06d7fb3` |
| ADR-0018 | 维护者澄清：源集合开放（封闭枚举 → 格式约束）+ 智能体升一等聚合维度 |
| 并行 | 阶段二（冻结竖切）派发；**三A**（基线核验）并行完成——Safari 17.4→**17.5** 修正、cargo-dist 存续核实、配置面核查；**阶段五**（设计基座 · brand-book specimen）并行派发 |
| 身份事件 | 占位 `a@b` 入公开历史 → 全历史洗净（main amend + 双分支同树 rebase）→ hook 升级机器强制身份 |
| 阶段五 | 交付（Python 参照实现 1,188 项对拍的三层验证）→ 四项设计裁决（light 双白意图化 / series 上下限 / ANSI16 接受 / 删 32）→ 二轮修复 |
| 阶段二 | 交付 → 规则 4 修订（先定流再定代）→ R1 对抗 4 必修 → 四大语义定案 → R3 又 4 必修 → 三轮微裁 → R4 2 必修（**均为契约措辞，归维护侧**）→ P5/P6 升一等公民 + 突变自证 |

## 2026-08-04 · 双线收口日

| 事件 | 结果 |
|---|---|
| 阶段二 R5 终审 | 2 必修（验证机构自身：P5 oracle 依赖 SUT、P6 模型抄实现——审计层按设计抓住共因）→ 类封死三条 |
| 阶段二 R6 | 1 必修（force_write 旁路）→ 守卫收紧为内容比较 |
| 阶段二 R7 | 维护者亲自跑机械核验（免派发）→ **通过，squash 合入 `02603ec`，阶段二关闭**。总账：7 轮、12 必修全灭、fold 引擎 R1 后零被攻破 |
| 阶段五复审 | R1 不通过（gamut C=0 豁免句为规格缺陷）→ 修规格源头 + P-tokens 全称性质 → R2 **通过**（突变自证抓出底端负 L 逃逸）。代码侧关闭，待维护者 specimen 验收 |
| 门序修正 | 冻结门按不可逆性拆分：**数据门**（spike① + DDL + fixtures）卡 M1；**界面门**（spike②③）卡 M2 |
| 三B | 交付六源 fixtures + 数据门评审包 → 维护者裁决（WITHOUT ROWID 采纳、spec 收敛十一项）→ **复审不通过 3 项** → 二轮裁决（cursor 键收敛 / harness mask / 逐字段保留） |
| 阶段六 | 交付三 transport + 文件锁 → 三轮裁决（Origin 缺失语义、锁保证边界、advisories 中间形态）→ CI 全绿 → **合入 `1396bea`，阶段六关闭** |
| 事故 2 | 维护者 `rm -rf` 阶段六 worktree 时跟随 NTFS junction 删掉整个 `internal/`（含其 .git）→ 从私有远端 `79c37f7` + 公开仓旧对象恢复 45 份 → 我方重写全部裁决与契约条款；执行者各自重出报告。**内部仓恢复"每批必推"纪律** |

## 常设资产（循环买回的，跟随一切后续阶段）

四场景收敛 + P5 跳过零副作用 + P6 证据正当性（契约直译共享模型、oracle 独立）+ P-tokens + 突变自证惯例 + 覆盖门禁（12 类角落）+ 契约↔模型对读（高危复审常设形态）+ 证据字段置值条款 + 内容比较写守卫 + 机器强制身份/路径守卫 + 宿主形态依赖类代码的跨形态 CI 验证要求。

## 公开 main 历史（全 noreply，零 AI 痕迹）

`7b1dbcc` 初始脚手架 → `fbee54b` workspace + 守卫 → `fbe8972` 根级 md 白名单 → `06d7fb3` NUL 守卫 → `02603ec` 收敛引擎 + adapter + 验证机构 → `1396bea` 跨端竖切 + 单 owner 锁。
