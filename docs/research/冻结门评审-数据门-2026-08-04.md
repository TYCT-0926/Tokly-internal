# 冻结门评审包 · 数据门（M0.5）

- 编制日期：2026-08-04（阶段三B 执行侧）
- 用途：数据门人工签核的单一评审入口。门序按[路线图](../路线图.md) 2026-08-04 修正注记：**数据门 = spike ① + DDL 冻结 + 六源 fixtures + 基线/配置面冻结 + 本评审**，卡 M1；界面门（spike ②③）卡 M2。
- 待审对象：main `02603ec`（阶段二合入）+ 分支 `feat/freeze-closure` `1e2652c`（本阶段 fixtures 交付，待复审合入）。
- **结论建议（供签核）：可过数据门**——附 3 项签核确认与未决项挂账清单，见 §五。

## 一、五件冻结物逐件状态

| # | 冻结物 | 状态 | 证据 |
|---|---|---|---|
| 1 | 可执行 DDL 冻结 | **已冻结**（附 2 项 DDL 裁决点，见 §二） | [数据库-schema](../数据库-schema.md) · migration v1（`02603ec`）· DDL 一致性测试 5 项 |
| 2 | 六源映射 golden fixtures | **本阶段交付**（`1e2652c` 待复审） | claude-code 真实 adapter 快照 + 五源 spec 契约快照 + 交叉验证，见下文 |
| 3 | 平台基线与 CSS 特性预算 | **已冻结**（2026-08-03 三A） | [阶段三A-独立核验](../reports/阶段三A-独立核验.md)：caniuse/MDN 双源逐项复核，Safari 17.4→**17.5** 修正回填 ADR-0009 |
| 4 | 配置面冻结 | **已冻结**（ADR-0010 + 三A 增补） | [ADR-0010](../adr/0010-配置子系统.md)：17+2 键骨架、优先级链、校验拒启动、`machine` = `<data_dir>/tokly/machine` UUIDv4（无硬件指纹）；三A 增补 `update_check`/`log_level`，API 凭据不进 config（推迟 M6） |
| 5 | DDL 一致性测试进 CI | **结构上已进，带已知偏差 B** | ci.yml 三平台 nextest 全量含该测试；internal 依赖见 §一.5 |

### 1. DDL 冻结

[数据库-schema](../数据库-schema.md) 为规范：16 表全 STRICT + pricing 区间重叠触发器 + 14 索引 + PRAGMA 固化集合；migration v1 全量落地并由 5 项一致性测试锁定（doc↔migration 的 `sqlite_schema` 逐对象规范化 diff、PRAGMA 块文档驱动逐项比对、版本漂移拒开、空迁移表拒绝、连接 PRAGMA 七项断言——[阶段二-冻结竖切](../reports/阶段二-冻结竖切.md)「逐命令验证」）。收敛契约（fold 五规则 + 事务边界 + P5/P6 全称性质）经阶段二七轮对抗磨到可执行精度后合入，ADR-0002 修订十余处条文是冻结门的设计产出（[阶段二-关闭](../reports/阶段二-关闭.md)）。

**遗留**：schema 文档自注的两处「待 spike ① 实测定稿」——`cache_size` 与窄表 `WITHOUT ROWID`——实测数字已得（§二）而 DDL 未动，属本门签核项。

### 2. 六源 golden fixtures（本阶段交付，`feat/freeze-closure` `1e2652c`）

**claude-code（经真实 adapter 接线，spec 全字段面）**：fixtures 扩至 5 文件 13 事件、adapter 测试 12 项、insta 映射快照重生成并逐字段人工复核。覆盖：流式重写 output 618→0 回落（禁取 max）、fork 字节复制去重与改写复制跨流冲突、cache_creation 明细与 aggregate 一致（20/10 精确拆档）/不一致（60+20≠100 → excluded + 留证）、**弱键 fallback**（无 requestId → uuid 键 + `identityQuality='weak'`）、**unverified-schema 双结构触发**（usage 非对象 / token 字段非整数 → excluded + `detail_key`=cliVersion 留证）、**subagent meta 缺失降级 `'subagent'`** 与 meta `agentType` 归因（`code-reviewer` 进 session_model_stats 二维键）、**旧版内嵌 sidechain**（`:sidechain` 会话后缀 + agent='sidechain'）、跳过白名单四型（user / isApiErrorMessage / `<synthetic>` / 无 usage）、坏时间戳与无稳定身份两类 unresolved-mapping 错误（记错不入库）、model/cwd/version 缺失降级（NULL 不造魔法值）、server_tool_use 双 charge（web-search/web-fetch）、reasoning 终态（thinking_tokens=40 ≤ output）、完整坏行与无换行残尾。

**五源 spec 契约快照**（`crates/tokly-core/tests/spec_contracts/`，20 case / 21 期望事件，标注「待各自 adapter 里程碑接线」）：输入样本 + 按 spec 手工推导的期望 UsageEvent 成对入库；harness 做域反序列化（token 不变量在类型层强制）+ `validate()` + **serde 逐字节往返**（拼错/未知字段即红，经突变自证）。要点覆盖：

- **codex**：legacy 差分（last 优先）、净输入顺序 clamp 双减（上游 100/40/60→净 0 用例冻结）、重复快照不重计（判别性覆盖）、rate_limits 独立采集三型（info null / auto-compact 合成 / 重复快照均零事件而快照照采）、agent_nickname 与缺失降级 `'subagent'`。**paginated 依「不猜」红线停下**（未决项 1）。
- **opencode**：Gen A/B 两代、total 方程四假设唯一解冻结「存储 input 已净 + 存储 output 不含 reasoning」并按不变量并 reasoning 入 output、fail-visible（无解 → `unresolved-token-semantics` + 冲突留证）、tool-computed 成本口径（×1e6 微美元、kind/provider、displayPolicy both、cost=0 且 tokens>0 仍 standard）、agent 空值不回退 mode、Gen B rollup 与 session.model 永不作事件源、parent_id → parentSessionKey。
- **copilot**：**已验证集合空集的 unverified 处置**（两通道事件均 excluded + 留证 + tokens 置零 + rawUsage 留存——冻结的是处置行为而非任何 token 语义）、OTEL chat span 身份/时间戳（[sec,nanos]→epoch_ms）、session-state 末快照胜出 + session 粒度、premium-request 数进 event_charges（3.6→3600 milli，永不入成本列）。
- **cursor**：estimated 路线（tokenCount 全零 → ceil(chars/4) 按 role 分池、0+`tokenCoverage` 四标注、`disabled`/`estimate-only`）、native 遗留行（standard/computed-only）、子代理父链（agent 恒 `''`）、auto 模式 model=NULL。
- **gemini-cli**：total 方程 inclusive/disjoint/判别失败三分支（uncached+tool 并入 input、thoughts 并入 output、`unresolved-cache-semantics` 专属码）、同 id 折叠后到 tokens 胜出、上一条消息 model 回退、二值 agent（main `''` / subagent `'subagent'` + 目录名父归属）。

**交叉验证**：六个独立复推 agent（五源逐字段重推导 + 公开产出卫生扫查，与构造者不同上下文）对全部冻结数值做了对拍——**token 算术、clamp、方程两侧、时间戳、微美元舍入、字符数零错误**；存活发现 7 项全部属「冻结纪律与 spec 措辞一致性」，已逐条裁决处置（修输入 2 处、补 UNRESOLVED/unresolved 声明 4 处、其余上报为 §四 spec 收敛点）。卫生扫查干净（无 internal 引用、无 AI 痕迹、全虚构数据、LF 行尾）。

对照路线图冻结物点名四件：Codex legacy ✓ / **paginated ✗**（未决项 1，判据见 §四）；OpenCode 两代 ✓；Copilot 格式版本表用例 ✓（以空集处置形态——表本身每格须实机样本，见未决项 13）；Claude cache_creation 裁决用例 ✓。

### 5. DDL 一致性测试的 CI 现状（如实说明）

- 测试按内容签名定位规范文档（公开代码零私有路径，阶段二评审修复 3）；**CI checkout 无 internal/ → 显式跳过并打印原因**；真实执行仅发生在持有 internal/ 的机器。
- 本阶段在 worktree 以 NTFS junction 挂载 internal 后复跑：两项一致性测试真实通过、无跳过通知（[阶段三B-冻结收口](../reports/阶段三B-冻结收口.md) 命令记录）；junction 用毕已移除。
- **残余风险**：CI 只拦「公开侧漂移进 PR」；「文档侧漂移」须靠本地门禁与维护纪律（文档改动后本地跑 nextest）。此为已知偏差 B，签核时知悉。

## 二、spike ①（Rust 数据竖切）验收逐条勾稽

路线图验收原文 → 实证（数字引自[阶段二-冻结竖切](../reports/阶段二-冻结竖切.md)与[阶段二-关闭](../reports/阶段二-关闭.md)）：

| 验收项 | 状态 | 证据 |
|---|---|---|
| proptest 收敛契约四场景终态一致 | ✓ | 交换/乱序/重放/重建各 96 cases；比较域 = 全部表全部逻辑列（仅排代理自增 id 与 sqlite_sequence）；关闭时 xtask ci 全链 **59/59**、deny 四项 ok（七轮总账：一轮 4 必修 → 七轮 0） |
| 快照含全部派生表（点名 session_model_stats） | ✓ | 快照七表逐字节，含 session_model_stats（model × agent 二维键）；复审攻击路径 d 专项验证 |
| STRICT 表开销实测 | ✓ 已定格 | 10 万行单事务：261,231 vs 261,120 rows/s——**零插入代价**，「全表 STRICT」无性能负债 |
| `cache_size` 在 rusqlite 下的真实收益 | ✓ 已实测，**待裁决** | default min 100.28ms / median 136ms vs -16000 min 100.26ms / median 122ms——统计上不可区分；bun:sqlite 栈「4–5 倍」在 rusqlite/Windows 该负载不成立（约 40MB 数据集疑被 OS 文件缓存吞掉差异）。PRAGMA 值未动 → **签核项 ①-a** |
| 窄表 `WITHOUT ROWID` 收益 | ✓ 已实测，**待裁决** | tool_calls 30 万行插入 353k vs 567k rows/s（**快约 1.6 倍**）、按事件查询 12.5 vs 9.1 µs（**快约 27%**）。DDL 未动 → **签核项 ①-b** |

附加资产（超出验收面，随 M1 常驻）：P5 跳过零副作用 + P6 证据正当性全称性质（契约直译共享模型、oracle 独立于 SUT）、突变自证惯例、覆盖门禁 12 类角落计数、证据字段全称置值条款、内容比较写守卫（阶段二-关闭「循环买回的常设资产」）。

## 三、门序修正的记录与理由

- **记录**：[路线图](../路线图.md) M0.5 节 2026-08-04 修正注记——原「三 spike 验收 + 五件冻结 + 评审通过，才准动工 M1」划除，改为按不可逆性拆分：**数据门**（spike ① + DDL 冻结 + 六源 fixtures + 基线/配置面冻结 + 评审）**卡 M1**；**界面门**（spike ② React UX + spike ③ 跨端）**卡 M2 的 Dashboard 大规模施工**。
- **理由（引原文）**：「M1 是纯数据层，其不可逆面（schema、契约、映射）已全部被数据门覆盖」；spike ②③ 冻结的是 UI/平台选型，「其不可逆性在 M2 才生效」。
- **上游依据**：[阶段二-关闭](../reports/阶段二-关闭.md)「三B（冻结收口）立即可开……阶段四（M1 全量）在冻结门过门之后——M0.5 是 M1 动工前提（路线图既定）」；[执行计划](../执行计划.md)并行规则「八不先于三B（R10）」不受影响。
- **评审侧核对**：成立。M1 清单（core schema / 摄取引擎 / claude adapter / pricing / config / doctor / report）逐项过——无一项依赖 spike ②③ 冻结的选型；数据门五件恰好覆盖 M1 全部入库后不可逆面。

## 四、全部已知偏差与未决项清单

公开仓侧每条未决项均以 `UNRESOLVED:` 标记或 README「Deliberately open」显式在册（harness 强制声明）；本清单为其权威对照与归属。

### A. fixtures / spec 未决项（按源）

| # | 条目 | 处置与归属 |
|---|---|---|
| 1 | **codex paginated 无 fixture**：spec 明言「页格式细节以 fixtures 冻结为准」且本机零样本；**且 spec 坑 7 与[二轮审查](审查-2026-08-02-二轮.md) §3 对「物理分页是否已落地」记载相反**（spec：0.146.0 已合并现役；审查：仍单文件 append，实际变化是行级 ordinal + SQLite 元数据投影 + `.jsonl.zst` 冷存储）。构造期望值必然掺猜测，依红线停下 | 阶段八 codex adapter 实机采样时一并裁决并补 fixtures；此前非 legacy 布局按 unverified-schema 处置（已冻结于 README/spec）。**spec 与审查的矛盾请维护者知悉** |
| 2 | codex eventKey：内容哈希字节编码（timestamp 形态、差分 vs 累计、分隔符）未定 → `UNRESOLVED:` 标记；spec eventKey 行「流内主键 path:offset / 跨流辅键内容哈希」**双键表述需收敛为单值定义**（复推确认：内容哈希是唯一与 ADR-0002 规则 4 fork 去重相容的读法） | spec 收敛批次（签核项 ②） |
| 3 | codex identityQuality：映射表无行（claude/copilot 均有显式行）。暂 `strong` + unresolved 声明；内容哈希键可合并两次真正相同参数的调用，正是该列要标的风险类，且该列入库不可逆 | spec 补行 + 理由（签核项 ②） |
| 4 | codex serviceTier：`thread_settings_applied` 载荷形状未实机验证，未 fixture | 阶段八实机采样 |
| 5 | codex cliVersion：映射表未列 → 契约冻结为不映射；如需上事件须补表行 | spec 收敛批次（可选） |
| 6 | opencode resolver「恰有唯一假设」字面规则：退化输入（cache=0 ∧ reasoning=0）下四假设全命中**同一映射结果**，按字面为「多解 → excluded」，fixtures 按「唯一性定义在映射结果」冻结 counted（意图无争议，字面推不出） | spec 补一句退化规则（签核项 ②） |
| 7 | opencode toolCall 展开式含 `title`，而 ADR-0002 tool_calls 逻辑列（childId/工具名/status/时长）无 title——**spec 与 ADR 冲突，按红线以 ADR 为准**，fixtures 不带 title | spec 展开式对齐 ADR，或 ADR 加列（维护者裁决，签核项 ②） |
| 8 | fail-visible/unverified 排除行的 `tokenQuality`：spec 只定义命中分支；暂 `native`（与已落地 claude 先例一致）+ unresolved 声明（opencode/gemini/copilot 各 fail-visible 行） | spec 补未命中分支取值（签核项 ②） |
| 9 | 排除行 token 置零 + rawUsage 留存 = 跨源通用惯例（claude unverified 先例）；rawUsage 精确序列化（键序/空白）adapter 决定，内容为冻结面 | 各 adapter 里程碑确认 |
| 10 | **copilot projectLabel 双读法**：映射行「context.cwd / repository」——平行对读（同表 kind/provider、eligibility/policy 行的既有惯例）→ repository；跨源 label→key 归一化惯例 → cwd。两读法下 projectKey 均唯一 = normalize(cwd)（已冻结）；**label 打 `UNRESOLVED:` 标记，不猜任何一边**（交叉验证的唯一 blocker，以此收案） | spec 定稿一读（签核项 ②） |
| 11 | copilot periodEnd（session 粒度）：域校验必填 → de-facto 冻结为 shutdown 时刻（与 month→月末、day→24:00Z 同构的「覆盖期终点」读法）；spec 缺一句显式语义 | spec 收敛批次 |
| 12 | copilot copilotVersion 分段作 session-state 格式判别 vs ADR-0014 澄清「发布版本号不作 schema 集合」存在张力；rawSchema 表示法未钉（OTEL 侧同） | M6 copilot 实机采样时连同语义表一并裁决 |
| 13 | **copilot 已验证集合 = 空集**：四通道零实机样本，token 语义表每格待样本证据。本阶段冻结的是空集处置行为（excluded + unverified-schema + 留证），非任何语义 | M6；不阻塞 M1（R10 判据不受影响） |
| 14 | cursor：`allThinkingBlocks`/`toolResults` 字符抽取的确切定义未钉（fixtures 只用 text 池规避，冻结数值不依赖它）；Best-of-N 落选分支的 excludeReason 词条未定义 | M6 cursor adapter |
| 15 | **gemini spec 自身示例 total=23146 不满足其自己的两个方程**（inclusive 13046 / exclusive 23286）——按 spec 自己的失败策略该示例行会被 excluded；fixtures 已用自洽数值（13046） | spec 示例修正（签核项 ②） |
| 16 | gemini counted 行 rawUsage 缺省：uncached+tool 并入 input 后 tool=96 不可逆，「折叠非有损（就留存而言）」的读法有映射表支撑但 spec 未明说 | spec 一句话背书 |
| 17 | claude spec 内部措辞：「已验证格式版本集合」节写 version「映射到 rawSchema」vs 映射表 version→cliVersion；adapter 现状 rawSchema=None | spec 措辞收敛 |
| 18 | claude `inference_geo`「暂只存档不展示」vs 映射表未列；adapter 现状忽略（阶段二 TODO ⑦ 在册） | spec 措辞收敛 / M1 顺带 |

### B. CI 门禁偏差

DDL 一致性测试的 internal 依赖与本地真执行机制（§一.5）——CI 防公开侧漂移、不防文档侧漂移。

### C. DDL 裁决点（spike ① 实测已得、DDL 未动）

`cache_size -16000` 存废、`tool_calls` 是否转 `WITHOUT ROWID`（§二）。

### D. 存量观察（非本批引入）

`crates/tokly-core/src/domain.rs:529` 测试数组含中文字面量「日志」作非法标识符样本（`02603ec` 已入库；如要求公开仓全无 CJK，可换任意非 ASCII 样本，不削弱测试）。

## 五、结论建议与理由（供人工签核）

**建议：可过数据门。** 附三项签核确认；未决项按 §四 归属挂账，不阻塞过门。

**理由**：

1. **门的目的已达成**——数据门冻结的是入库后不可逆的形状。DDL / 平台基线 / 配置面三件全冻结；收敛契约经七轮对抗打磨并由四场景 + P5/P6 + 突变自证常驻锁定；映射口径中 **spec 可唯一推导的部分已全部冻结**且经六路独立复推零数值错误；**不可唯一推导的部分全部显式 UNRESOLVED/未决，没有任何猜测值进入基准**——这正是「golden 里的猜测会成为永久错误基准」红线的达成形态，而非缺口。
2. **未决项不落在 M1 的不可逆面上**——M1 范围 = core + claude adapter + pricing/config/doctor/report。claude 映射 fixtures 经真实 adapter 完整覆盖；五源未决项全部挂在各自 adapter 里程碑（M2 opencode / M3 codex+gemini / M6 copilot+cursor）之前，届时实机样本先行、fixtures 同批冻结（ADR-0014 机制 + R10 判据均已在册）。
3. **两项 DDL 裁决点数字已备**，裁决是签核动作而非缺证据。评审侧建议：`cache_size` 维持 -16000（实测无害；数据集超过 OS 缓存后可能显益，翻案成本为零）；`tool_calls` 采纳 `WITHOUT ROWID`（插入 1.6×、查询 27% 双赢，需同批改 schema 文档 + migration + 一致性测试）。决定权在维护者。

**签核确认项（三项，签核即门关）——2026-08-04 已裁决（[阶段三B-裁决](../reports/阶段三B-裁决.md)），落地状态**：

- **①** **已裁决**：cache_size -16000 维持（schema 文档已打戳）；`tool_calls` + 孪生 `event_charges` 转 `WITHOUT ROWID`（schema 文档已修订，migration v1 与 DDL 一致性测试已同批落码于 feat/freeze-closure；`session_model_stats`/`watermarks` 未实测不动并注记）。
- **②** **已裁决**：十一项按裁决表落进 sources/ 五份 spec（全部措辞级，已冻结 fixtures 期望值零改动）——codex eventKey 单值化为内容哈希（编码细则仍留阶段八）+ identityQuality `strong` 补行（残余风险在册）+ cliVersion 显式不映射；opencode 退化唯一性规则 + 删 toolCall title（ADR 为准）；三源排除行 tokenQuality=`native` 补句；copilot label 定稿 cwd 原始值（repository 不映射）+ periodEnd=shutdown 时刻；gemini 示例修 13046 + 折叠留存背书；claude version→cliVersion 措辞收敛 + inference_geo 入表标不映射。§四 A 组 #2/3/5/6/7/8/10/11/15/16/17/18 就此关闭；**仍开放**：#1（paginated，阶段八）、#4（serviceTier 载荷，阶段八）、#9（排除行惯例各 adapter 里程碑确认）、#12/13（copilot M6）、#14（cursor M6）。
- **③** **已知悉**：偏差 B（CI 防公开侧漂移、文档侧靠本地门禁与维护纪律）与 D 组存量观察（CJK 字面量已随批替换为非 ASCII 非 CJK 样本）。

**反面清单**（如维护者判定以下任一为阻塞，则本建议不成立，改判「不可过」并回补）：copilot 空集意味着 M6 前该源无任何已验证语义（评审侧判断：按 ADR-0014 这恰是 fail-visible 的设计形态）；codex paginated 缺位（评审侧判断：spec 自身未定页格式，补造即违红线）；两项 DDL 裁决未决（评审侧判断：属签核动作本身）。三者均不在 M1 不可逆面上。
