# 安全与隐私政策

## 隐私立场（产品级承诺）

Tokly 是本地优先工具，隐私不是功能，是架构：

- **零上传**：不遥测、不分析、不回传。唯一的网络请求是获取公开定价表（LiteLLM，可离线缓存，可在设置中关闭）。
- **不存内容**：Tokly 只解析并存储 token/成本等**指标元数据**，永不存储你的对话内容、代码、文件路径以外的任何文本（见 ADR-0002 资源纪律）。
- **只读零侵入**：Tokly 对你机器上的 AI 工具只有读操作，永不修改任何配置、代理、环境变量（见 ADR-0003）。
- **凭据安全**：官方 API（如 Copilot）的授权凭据只存操作系统钥匙串，永不写入明文文件，仅用于你显式开启的功能。

## 报告安全漏洞

如果你发现安全问题（尤其是涉及读取越界、数据泄露、凭据处理的），**请勿开公开 issue**：

- 通过 GitHub Security Advisories 私密报告（Security → Report a vulnerability）
- 我们会在 72 小时内响应，修复后公开致谢（除非你希望匿名）

## 支持版本

仅最新 minor 版本接收安全修复（0.x 阶段）。
